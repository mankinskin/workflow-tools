Add a single-command `install-ctl guidance get <repository-url>` surface that clones the source repository with the system git binary, resolves a profile (explicit --profile or a synthesized implicit profile from bare --select repo-relative paths), builds and prints a plan, installs into --target (default the current directory) only when the plan is clean, and deletes the managed checkout afterward unless --keep-checkout is given. Reuses the existing plan/install/autofix code paths unchanged; adds no Git-client crate dependency.
Acceptance criteria remain focused on the durable provisioning record contract:\n\n- `<session-store>/sessions/<id>/provisioning.json` is the authoritative durable provisioning record.\n- Any OS-temp fallback under `copilot-capture-hook/provisioning-outcomes/` is best-effort diagnostic-only, may fail, and must not make hook execution fail or be required evidence for acceptance.\n- The ticket stays in-review; no implementation or spec change is required because current code already matches this contract.\n\nDecision/evidence note: the interview decision controls the durable-record contract, and the existing review history remains valid.
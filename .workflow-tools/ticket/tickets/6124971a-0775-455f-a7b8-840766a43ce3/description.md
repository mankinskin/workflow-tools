Implemented shared workspace/store root normalization for ticket/spec/rule stores, validated ticket create target roots so repo/store paths resolve into .ticket/tickets, removed the ticket workspace CLI surface, added path-aware ticket-mcp workspace resolution, removed ignored HTTP --workspace compatibility flags, deleted the repo .ticket-workspace marker, updated the workspace spec, regenerated rule-derived READMEs, and validated with cargo test/check plus spec refs validate.
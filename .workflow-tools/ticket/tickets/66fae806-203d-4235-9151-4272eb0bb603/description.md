Plan rule-generated instruction and prompt assets plus slash command skill for bootstrapping a minimally functional new domain store from one prompt, aligned with architecture-decisions and cross-store architecture constraints.

Acceptance criteria:
- rule-store source entries and generation targets are planned
- slash-command flow is specified end-to-end
- dedicated automated regression validation tickets exist

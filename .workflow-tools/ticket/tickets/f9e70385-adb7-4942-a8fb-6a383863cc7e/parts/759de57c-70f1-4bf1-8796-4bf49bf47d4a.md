Iteration review (this session) — returned to in-implementation.

FAIL (critical): AC7 ("no alternate write entry point can touch frozen content without the state-transition path") is disproven live. `TicketStore::update`'s legacy description write path (store.rs L655-700) bypasses `enforce_part_write_gate` entirely. Live repro on a scratch ticket: with the objective part `frozen: true`, `write_part` correctly rejected the write, but `update_ticket(description=..., description_mode="replace")` on the same ticket succeeded, producing a split-brain `description.md` vs. frozen `parts/<uuid>.md` — reproducing the exact shape of the original corruption incident this epic exists to prevent.

User decision: fix the freeze gate directly on this ticket (do not just open a separate bug ticket). Route the legacy description write path through the same frozen-part check `write_part` uses, then re-verify live: attempt `update_ticket(description=..., description_mode=...)` against a frozen objective part and confirm it is rejected with the same `FrozenPartWrite` semantics.

Secondary, not blocking this ticket but disclose explicitly: real `planned` tickets already in the store (e.g. 3a1ec9f8, c2d6a14a, d7141fac) show `frozen: false` because freeze only fires on a state *transition*, not retroactively. This is tracked separately by f65f2b32's migration validation spec (`vt-structured-ticket-entities-migration`) — note it in this ticket's description so AC1's "freeze exactly 5 planning parts on transition" claim isn't read as covering pre-existing planned tickets.

Also FAIL: AC8 test-api evidence — recorded execution does not match the epic-required `vt-structured-ticket-entities-rust` (zero executions). User decision: record real executions against the required id before closing.
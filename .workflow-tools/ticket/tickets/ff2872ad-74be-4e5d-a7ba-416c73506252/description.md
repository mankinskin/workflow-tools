Improved ticket-vscode error-state surfacing so provider failures now include caller context and the associated request details. The API client throws structured request errors with operation, method, URL, HTTP status, and response body, and the tree error state renders server URL, workspace, active filters, request info, and response details instead of the previous generic message. Validation: `npm run test:unit -- --runInBand --runTestsByPath test/unit/buildStateGroups.test.ts` passed; `npm run test:unit` passed; `npm run compile` passed.
Reference fix: sibling suite `structured-parts.release.spec.ts` in the same `e2e-release/` directory loads correctly and passes 3/3 — use its import depth into `viewer-api/viewer-api/frontend/dioxus/e2e/test_apis` as the reference for the correct relative path from `e2e-release/shared/common-viewer-suite.ts`.
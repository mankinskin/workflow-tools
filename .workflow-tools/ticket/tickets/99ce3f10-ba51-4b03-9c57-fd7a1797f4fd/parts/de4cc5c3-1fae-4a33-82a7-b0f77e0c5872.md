memory-viewers/ticket-viewer/frontend/dioxus/e2e-release/shared/common-viewer-suite.ts imports `loadAndInspectViewer` from an incorrect relative path (`../../../../../viewer-api/viewer-api/frontend/dioxus/e2e/test_apis`), which resolves to a nonexistent file (one directory level off). This causes the module to fail to load, so the release E2E suite (`ticket-viewer.release.spec.ts`, via `registerCommonViewerSuite`) executes 0 tests instead of running. Confirmed still failing on 2026-07-30.
- Investigate why `owning_workspace_for_path` (memory-api/tools/http/ticket-http/src/serve/handlers/tickets/types.rs:377-403) and `canonical_workspace_name_for_index_root`/`short_workspace_hash` (memory-api/tools/http/ticket-http/src/serve/registry.rs:448-518) diverge (path normalization, symlink resolution, or working-directory-relative vs canonical index_root).
- Make `ticket_ref_from_indexed` return a workspace id computed the same way as the registry's canonical id, for the ticket actually being returned.
- Existing tests asserting `ticket_ref.workspace` equality (listing_workspace.rs, lifecycle.rs, mutations.rs) must still pass or be updated consistently.
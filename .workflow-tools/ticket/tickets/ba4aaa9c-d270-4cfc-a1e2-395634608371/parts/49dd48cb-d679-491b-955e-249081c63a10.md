# Amendment: ticket repository registration

The extracted repository MUST be registered in the context-engine superproject as git submodule `ticket` from `https://github.com/mankinskin/ticket` on branch `main`.

This amendment supersedes the implementation-plan guidance that recommended consuming the extracted repository as a plain Git dependency. Dependency-resolution details do not replace the required submodule registration.
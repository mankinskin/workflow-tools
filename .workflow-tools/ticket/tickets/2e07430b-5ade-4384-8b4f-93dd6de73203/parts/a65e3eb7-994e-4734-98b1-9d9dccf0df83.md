## Validation observations (2026-08-14)

- `./target/debug/ticket.exe list --toon | head -n 1` exited `0` for both `ticket` and `head`; stderr was empty and contained no panic message.
- `./target/debug/spec.exe --help | head -n 1` exited `101` for `spec` and emitted a panic message. This is outside ticket `2e07430b`'s ticket-crate scope.
- `./target/debug/session.exe --help | head -n 1` exited `0` for both `session` and `head`, with no panic message.
- `audit.exe` and `rule.exe` were not built in this worktree, so no early-close result was available for those CLIs.
- `cargo test -p ticket --all-features --no-run` completed successfully. The complete suite could not reach a `test result:` line in this terminal: each run exited `130` before writing output, including a raw Cargo fallback with redirected output.

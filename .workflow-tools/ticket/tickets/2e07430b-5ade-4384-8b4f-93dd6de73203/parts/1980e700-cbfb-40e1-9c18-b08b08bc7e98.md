- `./target/debug/ticket.exe list --toon | head -40` produces truncated output without a panic and exits 0; the exit code is asserted in the validation command or test.
- Equivalent early-close pipeline coverage verifies that the ticket CLI handles a broken pipe cleanly.
- The ticket notes record observed results for `spec`, `audit`, `rule`, and `session` under an early-closing stdout consumer.
- Any shared CLI-runtime fix includes focused regression coverage where the repository test harness supports it.

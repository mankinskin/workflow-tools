Implemented via delegated Implement Agent worker on branch agent/b0bc0a92-6185-43a2-9959-ca6bd2084b76/move-health-6d4ac04f (nested-repo worktree .worktrees/b0bc0a92-6185-43a2-9959-ca6bd2084b76/move-health-6d4ac04f inside workflow-tools; also reachable as workflow-tools submodule inside the outer session worktree at that same relative path).

Files changed:
- spec/crates/spec-api/Cargo.toml (criterion dev-dep + [[bench]] move_health)
- spec/crates/spec-api/benches/move_health.rs (new)
- session/crates/session-api/Cargo.toml (criterion dev-dep + [[bench]] move_health)
- session/crates/session-api/benches/move_health.rs (new)
- Cargo.lock (regenerated)

Behavior: move_scenario_matrix Criterion group per domain, driving plan_move_preflight / execute_move_with_journal / rollback_move_with_journal.
- spec-api: full matrix (entity_count in {1,25,100,500} x topology {none,internal,crossing} x links_per_entity {0,1,5,20} x phase {preflight,apply,rollback}), links via real EntityStore::add_edge (kind related), since SpecMoveDomain::related_entities reads real edges.
- session-api: matrix collapsed to entity_count {1,25,100,500} x phase {preflight,apply,rollback} only -- no link axis, because SessionMoveDomain::related_entities always returns empty MoveReferences (sessions have no edge/link model). Collapse is structural, not a shortcut.

Validation evidence (compile, clean):
- cargo check -p spec-api --benches -> Finished, 0 errors (1 pre-existing unrelated warning in store_index_tests.rs)
- cargo check -p session-api --benches -> Finished, 0 errors (pre-existing warnings only)

Validation evidence (bounded smoke runs, timeout policy applied per new mandatory runtime discipline):
- timeout 170 cargo bench -p spec-api --bench move_health -- --test: 17/75 scenarios Success (all 1entities_* combos + 25entities_no_links_0links_* + 25entities_internal_links_1links_preflight), killed at 170s deadline mid 25entities_internal_links_1links_apply, no failures observed before cutoff.
- timeout 150 cargo bench -p session-api --bench move_health -- --test: 11/12 scenarios Success (all of 1/25/100entities_* + 500entities_preflight), killed at 150s deadline mid 500entities_apply, no failures observed before cutoff.

Gap: neither bench completed its full scenario matrix within the bounded smoke timeouts (fixture setup cost scales with entity count); every scenario observed before each deadline passed. No functional defect found. A full non-smoke statistical cargo bench run (the literal 'produces Criterion output' wording of some acceptance criteria) was not attempted, consistent with the mandatory bounded-timeout policy established after the d7099a0c session overran ~2h polling a background run.

Git status of the nested worktree: only the intended Cargo.toml/Cargo.lock/benches/move_health.rs changes are present; no untracked Criterion output directories were left (smoke --test mode does not emit target/criterion report artifacts).
Context-enrichment re-verification (independent of the ticket's own prior review text).

## Acceptance criterion (verbatim, from the objective part's "## Acceptance Criteria" section)
"cargo bench -p spec-api --bench move_health and cargo bench -p session-api --bench move_health both run to completion and produce Criterion output covering every scenario in the shared matrix."

## Verdict: UNMET — ticket stays in-review

The prior review part's own recorded evidence already concedes this:
- spec-api: 17/75 scenarios observed complete before the 170s bounded smoke-timeout killed the run mid `25entities_internal_links_1links_apply`.
- session-api: 11/12 scenarios observed complete before the 150s bounded smoke-timeout killed the run mid `500entities_apply`.
- No full, un-truncated `cargo bench` invocation was ever attempted for either crate.

"Run to completion and produce Criterion output covering every scenario in the shared matrix" is not satisfied by a partial, deliberately time-boxed smoke run, no matter how clean the partial results are. Zero test failures observed is evidence of correctness-so-far, not evidence of completion. This is the sole gap and it is sufficient by itself to block closure to `done`.

## Independently verified: implementation content matches the claim
Read (not edited) both files directly at the worktree path:
- `spec/crates/spec-api/benches/move_health.rs` — confirms the full entity_count x topology x link_density x phase matrix, driven through `SpecStore`/`EdgeRecord`, matching the review's description.
- `session/crates/session-api/benches/move_health.rs` — confirms the collapsed entity_count x phase matrix (explicit doc comment: "SessionMoveDomain::related_entities always returns an empty MoveReferences ... so unlike the ticket and spec domains this matrix has no link-topology or link-density dimension"), matching the review's description of the structural (not corner-cut) collapse.

## Correction to the prior review's "uncommitted" claim (stale, superseded by later commits)
The prior review part states "The code lives UNCOMMITTED in a worker worktree ... Git status of the nested worktree: only the intended Cargo.toml/Cargo.lock/benches/move_health.rs changes are present" (i.e., working-tree diffs). That is no longer the current state. Direct inspection of the worktree at `.worktrees/b0bc0a92-6185-43a2-9959-ca6bd2084b76/move-health-6d4ac04f/workflow-tools` on 2026-09-02 shows:
- `spec` submodule: on branch `agent/b0bc0a92-6185-43a2-9959-ca6bd2084b76/move-health-6d4ac04f`, clean working tree, commit `39a54b06` "feat(benchmarks): add move-health scenario-matrix benchmarks for spec domain" (2026-09-02 11:48:54 +0200).
- `session` submodule: same branch, clean working tree, commit `32157ef7` "feat(benchmarks): add move-health scenario-matrix benchmarks for session domain" (2026-09-02 11:49:10 +0200).
- Outer `workflow-tools` superproject: detached HEAD, but its current tip commit `bff8354` ("chore: update dependencies and subproject commit references") records gitlinks for both `spec` and `session` pointing at exactly those two commits, and `git submodule status` shows no `+`/`-` drift.

So as of this re-verification, the benchmark code is committed on feature branches in both nested submodules and the outer repo's gitlinks are committed to match — it is not raw uncommitted working-tree state. It remains unmerged into any mainline branch and the outer `workflow-tools` repo is on a detached HEAD, so "pending merge" still applies, but the specific "uncommitted" framing in the prior review part is inaccurate as of now and should not be relied on. This correction is recorded for traceability; it is not the reason for the unmet verdict — the unmet acceptance criterion (partial-only bench completion) is.

## Disposition
Ticket remains `in-review`. Do not close to `done`. Before closure, either:
(a) run each `cargo bench -p <crate> --bench move_health` to full, un-truncated completion and record that every scenario in the stated matrix produced Criterion output, or
(b) if a full run is judged infeasible within available time budgets, get an explicit acceptance-criteria amendment (frozen part is immutable — use `write_amendment`) narrowing "run to completion" to an agreed bounded/partial-coverage standard, before treating the criterion as satisfied.
## Implementation route

1. Update the runtime command catalog/registry only where the final command surface needs discoverability; keep profile documents and user-created corpora out of the binary artifact registry.
2. Add operator documentation at the install-ctl and workflow-tools levels, using one canonical contract section and short links from secondary docs rather than duplicating safety rules.
3. Include command examples that show `plan` before `install`, clean-audit gating, explicit autofix plan/apply, repository/user/system/explicit targets, submodule fallback, and fixture execution.
4. Document stable diagnostic categories and expected exit behavior for successful plans, blocked plans, failed install, stale autofix, and post-audit rollback.
5. Add a generated-catalog check and a guidance-link audit to the documentation validation path. Repair any new links before completion.
6. Update README/COMMANDS/operator guidance only after W5-W7 expose the final names and output schema; do not freeze hypothetical CLI syntax prematurely.

## Ownership boundaries

- This ticket owns discoverability, operator docs, catalog projection, and documentation validation.
- Runtime behavior remains owned by `5584da3b`, `76abb9a1`, and `07601b9b`.
- Do not add a second profile database, copy full ticket bodies into docs, or claim network smoke tests are deterministic acceptance.

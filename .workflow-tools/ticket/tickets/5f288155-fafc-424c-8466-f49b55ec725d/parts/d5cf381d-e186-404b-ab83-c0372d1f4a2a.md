## Validation contract

### Documentation and catalog checks

1. `cargo run -p install-ctl -- catalog --check` must pass after any `artifacts.toml` or catalog renderer change.
2. `audit links workflow-tools` must pass with zero blocking documentation/guidance findings.
3. Run a documentation command-surface check that extracts every documented `install-ctl guidance` command and verifies it appears in `--help` or the owning runtime contract.
4. Review examples against the final serialized plan/report schema and assert no example uses host-specific paths or undocumented flags.
5. Verify user-created profiles are documented outside the runtime binary registry and catalog output.

### Cross-ticket evidence

6. Link the final docs to tickets `5584da3b`, `76abb9a1`, `07601b9b`, and the relevant work-package paths without embedding ticket/spec/code dependencies into publishable guidance.
7. Record validation specs/executions linked to ticket `5f288155` and include catalog/audit command output plus the reviewed documentation paths.

### Exit criteria

AC1-AC9 have either command-backed or recorded review evidence, catalog projection is current, guidance links are clean, and the documentation accurately describes only behavior validated by W5-W7.

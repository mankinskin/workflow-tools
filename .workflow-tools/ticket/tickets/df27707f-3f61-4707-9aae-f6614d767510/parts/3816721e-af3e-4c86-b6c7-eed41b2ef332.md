- `.agents/instructions/testing/assertions.instructions.md` requires explicit ordered assertions for order-sensitive API contracts.
- The instruction requires stable ordered fixtures and rejects random v4 UUID fixtures when order is under test.
- The instruction cites the `BTreeSet` board-aggregation first-seen-order defect.
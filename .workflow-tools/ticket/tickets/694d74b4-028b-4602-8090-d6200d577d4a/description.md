Removed the remaining TS-only fallback paths from ticket-vscode host/provider code, switched ticket navigation to VS Code external-URI routing, and updated provider tests to inject a core mock so the core contract is exercised directly. Validation: `npx jest --config ./jest.config.ts --runInBand test/unit/buildStateGroups.test.ts` and `npx jest --config ./jest.config.ts --runInBand test/unit/extensionCommands.test.ts` both passed; parser checks on changed TS files are clean.
Coordinate planning and delivery for ticket graph rendering, validation-aware dependency requirements, and audit/board enforcement built on the existing ticket graph and test-api evidence store.

Scope:
- define graph rendering contracts for CLI and reusable outputs
- define validation-aware dependency semantics tied to test-api evidence
- define audit and board health enforcement for dependency validation state

Acceptance criteria:
- child tickets cover graph rendering, dependency validation model, and audit/board enforcement
- planning specs exist for each area and link the canonical ticket paths
- ticket and spec health pass after creation

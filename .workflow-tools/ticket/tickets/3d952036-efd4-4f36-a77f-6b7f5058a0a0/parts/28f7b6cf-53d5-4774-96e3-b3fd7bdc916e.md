Iteration review (this session) — returned to in-implementation.

FAIL: AC5 claims "compile-time missing-field error" for description_mode, but `UpdateTicketInput.description_mode` / `UpdateTicketBody.description_mode` / `UpdateArgs.description_mode` are all `Option<...>` (mcp/http/cli types.rs, operations.rs) — enforcement is a runtime check in store.rs, not compile-time. User decision: add real compile-time enforcement (e.g. a required-field builder/typestate) before closing, not just a wording fix.

CRITICAL cross-cutting finding (shared with f9e70385): `TicketStore::update`'s legacy whole-description write path (store.rs L655-700) calls `TicketFs::write_description` directly and never calls `enforce_part_write_gate`. Live-reproduced: `update_ticket(description=..., description_mode="replace")` succeeds even when the objective part is frozen, diverging `description.md` from the frozen `parts/<uuid>.md`. This ticket owns the legacy description write path — route it through the same frozen-gate check `write_part` uses (or reject outright when the resolved objective part is frozen).

Also FAIL: AC7 test-api evidence — bespoke `exec-3d952036-20260730` only; epic-required `vt-structured-ticket-entities-rust` has zero executions. User decision: record real executions against the required id before closing.
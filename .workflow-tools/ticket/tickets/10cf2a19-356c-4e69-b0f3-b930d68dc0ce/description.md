Add ticket-http parity for the blocker-tree and recently-unblocked workflow surfaces so browser clients can consume the same ordering and tree data as the CLI and MCP surfaces. This work should define stable HTTP payloads for actionable next, upstream blockers, downstream unlock trees, and frontier-focused follow-up views while preserving server-authored workflow order and ranking metadata from ticket-api.
Execution started 2026-08-19. Physically copied session-api, session-worktree-provision, session-cli (-> src/cli.rs), session-mcp (-> src/mcp.rs + src/mcp/server.rs), and session-capture-hook (-> src/bin/session-capture-hook/{main,args,logging}.rs) from memory-api into workflow-tools/session, following the rule/ticket/spec single-crate-with-feature-gated-bins pattern (features: cli, mcp, capture-hook).

session-api Cargo.toml: repointed feedback-api and test-api from path deps to `git = https://github.com/mankinskin/memory-api` bridges (both remain unextracted, same pattern as rule-api used). rule-api/spec-api/ticket-api already resolve via their existing extracted-repo git deps.

session-worktree-provision required no dependency changes (already fully git-based).

Consumers repointed from path to `session-api`/`session-worktree-provision` git deps on `https://github.com/mankinskin/session`: memory-api/crates/audit-api, memory-api/crates/memory-matrix, memory-api/crates/session-workspace-resolver (stays in memory-api), memory-api/crates/session-record-merge (stays in memory-api), memory-api/tools/cli/audit-cli, memory-api/tools/mcp/mcp-toolmon, tools/worktree/worktree-ctl. No source-level changes needed in these consumers (unlike rule, which needed matrix.rs/cli.rs import updates) since none of them called into session-cli/session-mcp as libraries.

Legacy directories deleted: memory-api/crates/session-api, memory-api/crates/session-capture-hook, memory-api/crates/session-worktree-provision, memory-api/tools/cli/session-cli, memory-api/tools/mcp/session-mcp.

Root Cargo.toml: removed the 5 deleted paths + session-cli/session-mcp tool paths from workspace members; added workflow-tools/session + its two crates; added `session-workspace-resolver` path patch under the existing memory-api patch block (needed because workflow-tools/session's Cargo.toml also declares it as a git dep); added new `[patch."https://github.com/mankinskin/session"]` block (session, session-api, session-worktree-provision).

tools/install/artifacts.toml: repointed session-cli, session-mcp, session-capture-hook, and the hook-copilot-capture doc-reference entry from memory-api paths to workflow-tools/session with appropriate features arrays, mirroring the rule/ticket/spec fix from the prior install-tools.sh regression.

Fixed 3 stale hardcoded legacy-path string literals inside session-api's own copied tests (store_tests/workflow/snapshot_and_handoff.rs) that asserted handoff target-file existence against the old `memory-api/crates/session-api/...` path (both forward-slash and backslash forms) — updated to `workflow-tools/session/crates/session-api/...`. Also fixed session-capture-hook's `include_str!` fixture path depth (bin moved one directory deeper: `../tests/...` -> `../../../tests/...`) and the two e2e tests' hardcoded `.join("memory-api/crates/session-capture-hook/Cargo.toml")` reference (-> `workflow-tools/session/Cargo.toml`). Updated tools/agent-hooks/session-capture-stop.sh's stale dev-mode cargo-run fallback (wrong manifest path, wrong bin name `copilot-capture-hook` that never matched the real `session-capture-hook` binary, missing `--features capture-hook`).

Validation: `cargo build --manifest-path workflow-tools/session/Cargo.toml --all-features` clean; `cargo test -p session-api -p session-worktree-provision` 44+242 passed; `cargo test --manifest-path workflow-tools/session/Cargo.toml --all-features` all pass except 3 known-pre-existing environment-dependent mcp server test failures (verified identical failures reproduce against the untouched main-checkout session-mcp crate, confirming no regression); `cargo build --workspace` clean; consumer crate tests (audit-api, memory-matrix, audit-cli, session-record-merge, session-workspace-resolver, worktree-ctl) pass except memory-matrix's pre-existing `ticket_get_mcp_cell_is_wired_and_passes`/`every_cell_records_an_execution_with_duration` flakes (same baseline failures reproduced against untouched main checkout) plus one additional flake (`executions_are_persisted_in_the_workspace_test_store`) confirmed to pass in isolation (parallel-test-timing flake, not a regression). install-tools.sh re-verification in progress.
Extraction complete and merged to main. Pushed commits: session repo (c1fddb3, real content), workflow-tools (ef06ac8, merge reconciling with an unrelated concurrent board-show fix), memory-api (d5dd8d3f, merge reconciling the same), context-engine root (1767f5bb).

Reconciliation note: the main checkout had 3 unpushed local commits (including an unrelated memory-api/workflow-tools change for ticket b83d2e14, "board show recommendation-parity fix") that diverged from the session-worktree's branch. Resolved by: (1) fast-forward-pushing the session extraction directly to origin/main first (worktree branch's parent matched origin/main exactly), (2) merging origin/main into memory-api's and workflow-tools' locally-diverged branches (real 3-way merges, not gitlink-only), catching and correcting one gitlink misresolution where the merge tool reverted workflow-tools' `session` submodule pointer back to the pre-extraction placeholder commit (fixed via checkout + amend before push), (3) rebasing the main checkout's 3 local commits onto the newly-pushed origin/main (clean, no conflicts beyond the already-resolved submodule state), (4) pushing the reconciled main.

Recovered-during-validation gaps (initial physical copy missed these; all fixed before pushing): `crates/session-api/examples/smoke_delegation_cost.rs`, 6 `crates/session-api/tests/*.rs` integration test files, and `tools/cli/session-cli/tests/cli.rs` (ported to `workflow-tools/session/tests/cli.rs`, `session_cli::` import updated to `session::cli::`). Fixed 3 additional stale hardcoded `memory-api/crates/session-api/...` path assertions inside the recovered `handoff_folder_storage.rs`/`handoff_roundtrip.rs` tests, and a `.benchmark/10d21210/baseline` relative-path depth bug in `delegation_cost_benchmark.rs` (crate moved one directory deeper: `../../../` -> `../../../../`).

Deliberate exclusion: removed the `gitignore_tracks_durable_ginores_local` test from `gitignore_policy.rs` (and its now-unused `find_repo_root` helper) — it asserted policy about the *consuming repository's* ambient root `.gitignore` by walking up from cwd to the first `.git`, which now stops at the extracted crate's own submodule boundary instead of context-engine's root; this is a monorepo-integration fact, not a session-api behavior, mirroring the `install_contract_sync.rs` exclusion from the `rule` extraction. The self-contained `runtime_init_writes_an_idempotent_store_local_ignore_rule` test (validates session-api's own generated `.session/.gitignore`) was kept.

Validation (final, all green except documented pre-existing flakes): `cargo build --manifest-path workflow-tools/session/Cargo.toml --all-features`; `cargo test -p session-api -p session-worktree-provision` (243+ tests including all recovered examples/tests); `cargo test --manifest-path workflow-tools/session/Cargo.toml --all-features` (cli/mcp/capture-hook features, including the new `cli.rs` integration test, all e2e hook tests); `cargo build --workspace` clean at the time of the worktree validation pass. Consumer tests (audit-api, audit-cli, memory-matrix, session-record-merge, session-workspace-resolver, worktree-ctl) pass except the already-documented pre-existing memory-matrix `ticket_get_mcp_cell_is_wired_and_passes` / `every_cell_records_an_execution_with_duration` flakes (reproduced identically against the untouched baseline).

Known follow-up (host environment, not code): a final `cargo build --workspace` re-run from the main checkout after merging failed with `LNK1180: insufficient disk space to complete link` (host disk at 824MB free / 1.9TB) — an environment resource constraint unrelated to the session extraction's correctness, since all the same crates/binaries were already verified building cleanly earlier in the same session before disk filled. Not yet re-validated post-disk-cleanup; flagged for the user rather than resolved autonomously (freeing disk space, e.g. clearing `target/`, was left to the user's discretion).

`tools/install/artifacts.toml` was updated for session-cli/session-mcp/session-capture-hook (mirroring the earlier rule-cli/rule-mcp fix) but the full `install-tools.sh --offline` re-run was NOT re-verified end-to-end after the final merges/push (a prior attempt failed for the expected, previously-documented reason: a brand-new git dependency edge to `https://github.com/mankinskin/session` needs one real network fetch to warm Cargo's git cache before `--offline` installs can resolve it; this is the same class of issue resolved during the `rule` extraction by pushing real content first, which has now been done for `session` too).
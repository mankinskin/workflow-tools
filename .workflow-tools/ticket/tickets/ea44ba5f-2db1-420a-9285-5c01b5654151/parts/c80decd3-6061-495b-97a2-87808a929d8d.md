- The ticket names a chosen answer for each open design question, or explicitly
  defers the question to a named follow-up ticket.
- The derived-edge model distinguishes persisted dependency edges from derived
  latent and execution-order edges, and states that execution-order edges use
  the inverse direction of dependency edges.
- A worked example on the `7bc328d7` epic subgraph describes the resulting
  closure, reduction, and wave assignment.
- Rendering integration is stated: the output is consumable by the conventions
  in ticket `cd7aceca` without further transformation.
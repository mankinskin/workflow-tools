- Retry-limit guidance separately defines handling for compile/build failures and test failures.
- Mechanical compile/build failures permit a local iterative repair cycle.
- Test failures preserve the existing one-self-fix-retry then escalate rule.
- The clarification prevents abandoning partially applied work solely because a build failed twice.
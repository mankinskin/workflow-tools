The existing hook-capture E2E harness reaches `copilot -p` but has reported exit 1 from live payload schema drift that predates Subagent capture work. The pre-existing assertion contract is common keys `timestamp`, `hook_event_name`, `session_id`, `cwd`, `transcript_path`; event keys are `source` (SessionStart), `prompt` (UserPromptSubmit), `tool_name`, `tool_input`, `tool_use_id` (PreToolUse), `tool_response` (PostToolUse), and `stop_hook_active` (Stop). A completing retained run must record the exact `DRIFT: <event> is missing documented field(s): ...` lines and observed keys before reconciling the contract.

The fixture prompt performs a tool call but does not dispatch a sub-agent. `required_events` contains only SessionStart, UserPromptSubmit, and Stop, so missing SubagentStart/SubagentStop captures are informational. Update the fixture to dispatch a sub-agent and require both lifecycle events, asserting `agent_id` and `agent_type`, plus `stop_hook_active` for SubagentStop.
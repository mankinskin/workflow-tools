1. Fresh capture/MCP sessions use a minted or capture/provisioned UUID as `session_id`, never a slug/topic.
2. Bootstrap paths treat `session_id` as the sole identity; `workspace_session_id` has no identity role.
3. `init`/MCP emits only UUID `session_id`; any topic label is explicitly non-identity.
4. New runtime context persists at `.session/sessions/<session_id>/context.json`, never a slug-keyed directory.
5. Provisioning, capture, and `init` resolve the same established UUID for a worktree.
6. UUID-only commands reject slug-shaped input early with actionable errors.
7. Active-marker reuse is validated so a prior conversation cannot leak its identity.
8. Tests cover stale-marker rejection and provisioned-worktree UUID parity.
9. Slug-keyed legacy runtime-context directories remain readable backward-compatibly.
10. `epic-kickoff-8fdfe135` and `structured-ticket-entities-iteration` remain untouched — they are VALID records, not corruption, and must not be deleted, migrated, or restructured.
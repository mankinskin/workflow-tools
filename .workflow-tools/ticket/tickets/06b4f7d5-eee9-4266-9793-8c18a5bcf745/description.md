# Objective

Group the log schema, log API, and log viewer migration tickets under a single tracker so the subsystem has a coherent parent in the ticket graph.

# Scope

- Log viewer migration work.
- Log schema and API improvements.
- Cross-tool log ingestion and validation work.

# Handoff bfda2709-5a9a-4e7c-81c7-6fdb288b1670

**Objective:** Deduplicate the stale board module in memory-kernel (ticket c320708d) — confirm there are no live callers of memory-kernel/src/storage/board.rs, then either delete it or make it re-export the memory-api definitions, and prove cargo check --workspace passes.

CONTEXT ANCHORS (moved here because the context_anchors field rejects free-text entries — it requires each entry to be an existing workspace-relative path):
- main is at b84506a3 and in sync with origin/main. Submodule memory-api commit c6eb0a3a was pushed to its own origin BEFORE the superproject, repairing a pointer that previously resolved only on this machine. All five submodules are healthy.
- Session commits: bb0b0ca3 feat(tooling) worktree helper script; a415b25c chore(tickets); 4909db52 fix(tooling) drop harmful submodule deinit from teardown; b84506a3 chore(tickets) protocol fix evidence.
- LIVE board module: memory-api/crates/memory-api/src/storage/board.rs, 425 lines, defines BoardEntry, BoardEntryStatus, BoardConfig, BoardSnapshot, ActiveWorktree, BoardHistorySnapshot, BoardCleanPreview, BoardCleanResult, ReconcileAction, BoardReconcileResult, BoardError.
- STALE duplicate: memory-kernel/src/storage/board.rs, 295 lines, same set minus ActiveWorktree, off the live call path.
- Divergence introduced by ticket c060bf94, which added session_id/worktree_path/branch to BoardEntry plus ActiveWorktree, a WorktreeConflict error, and board_worktrees surfaces to the memory-api copy only. The user confirmed the duplication is NOT intentional.
- A full copy of this handoff is already appended to ticket c320708d's description under the heading 'Handoff package (from the worktree-helper session)'.
- Tooling: tools/worktree/worktree.sh provides new, list, rebase, merge, remove, doctor; --dry-run on every mutating subcommand; refuses destructive operations from inside a linked worktree.

**Target tickets:** c320708d-8dc1-46e2-ab5d-9903460ae27a

**Target files:** memory-kernel/src/storage/board.rs, memory-api/crates/memory-api/src/storage/board.rs

**Validation:**
```json
[
  {
    "validation_spec_id": "workspace-cargo-check",
    "required": false,
    "command": "cargo check --workspace"
  },
  {
    "validation_spec_id": "worktree-script-doctor",
    "required": false,
    "command": "bash tools/worktree/worktree.sh doctor"
  },
  {
    "validation_spec_id": "worktree-script-parse",
    "required": false,
    "command": "bash -n tools/worktree/worktree.sh"
  }
]
```

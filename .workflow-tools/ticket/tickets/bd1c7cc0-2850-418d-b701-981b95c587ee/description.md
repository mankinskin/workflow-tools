Plan continuous auditing to detect stale, conflicting, or low-value entries across spec/rule/ticket stores using activity, validation, and feedback signals.

Acceptance criteria:
- health metric taxonomy is specified with scoring model
- operator feedback loops are specified for triage and cleanup
- auditing tickets include validation and rollout gating

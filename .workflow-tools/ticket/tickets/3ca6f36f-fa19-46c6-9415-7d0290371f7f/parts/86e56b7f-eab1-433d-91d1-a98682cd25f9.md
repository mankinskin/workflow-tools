Create a user-invocable GPT-5 mini agent that selects and runs exactly one terminal command for simple requests. Its workflow is: discover the fixed tool inventory from frontmatter and runtime schemas; plan and select one app command; gate execution with vscode/askQuestions unless the command is one unambiguous, bounded, read-only command; then return the exact command and result. Complex, destructive, broad, networked, or unclear commands always require confirmation.
# Main integration validation

Validated on main `374bcdf7cc71fd701081686d8d852ca9eebfa0c9`; `memory-api` gitlink `a2161c974ddd7d0a8577b47ce58051496f2b5142`. All five recorded submodule gitlinks are contained by their checked-out histories.

## Per-crate results

- `session-api`: 226 passed (plus 5, 4 with 1 ignored, 1, 9, 6, 4, and doc-test 0); no failures.
- `session-capture-hook`: 11, 14, and 4 passed; no failures.
- `session-workspace-resolver`: 21 passed; no failures (plus doc-test 0).
- `session-worktree-provision`: 44 passed; no failures (plus doc-test 0).
- `worktree-ctl`: 10, 14, and 16 passed; no failures. The requested expected count for the final suite was 15, so actual coverage is one test higher, not a shortfall.

Focused main-store check: `cargo test -p session-capture-hook e2e_user_prompt_preserves_anchor_session_store_snapshot` passed (1 test). The test asserts `assert_eq!(before, after, "UserPromptSubmit must not add, modify, or delete main-checkout .session paths; changed paths: {:?}", ...)`. Artifact read-back after the invocation: `git status --short -- .session` reports only ` M .session/sessions/epic-kickoff-8fdfe135/context.json`, a pre-existing concurrent-agent change, not attributable to the integration.

## Acceptance criteria

1. MET - nested provisioning path and branch: `new_creates_nested_worktree_and_branch`; provisioning suite 44 passed.
2. PARTIAL - focused hook snapshot test proves main `.session` contents unchanged and manual artifact read-back found only the known pre-existing path, but the fixture does not execute the criterion-requested `git status --short .session` command.
3. MET - resolver tests `nested_worktree_discovers_without_main_checkout_registry`, legacy discovery, missing, and ambiguity coverage; resolver suite 21 passed.
4. MET - `multiple_nested_slugs_return_sorted_ambiguity_error` and `nested_slug_candidates_are_ambiguous_in_lexicographic_order`; relevant suites passed.
5. MET - `e2e_user_prompt_preserves_anchor_session_store_snapshot` passed; source assertion covers no main-checkout path additions, modifications, or deletions.
6. MET - session-api tests `federated_query_unions_main_nested_and_legacy_with_worktree_duplicate_winner`, `federated_tool_metrics_and_backfill_scan_main_nested_and_legacy_sessions`, `federated_handoff_pickup_and_backlog_find_worktree_only_nested_and_legacy_handoffs`, and `federated_ticket_relation_finds_worktree_only_session_and_skips_malformed_source`; session-api suite passed.
7. MET - `session_store_activity_prevents_reclaim_for_nested_worktree_record`; provisioning suite passed.
8. MET - `all_lifecycle_ops_support_dry_run`, nested/legacy worktree contract coverage, and worktree-ctl 10/14/16 suites passed.
9. MET - named guidance files document full UUID nested paths/branches, no main-checkout assignment index, and legacy transition support.
10. MET - all requested targeted crate suites passed; known unrelated failures were not run or modified.

Open review notes, not fixed: spec `22298b0d` AC8 dual-layout-removal clause lacks a lifecycle-audit command; the spec remains draft without PolicyRule or ValidationSpec links; five deliberately unchanged docs still state flat layout; spec tooling still fails verbatim with `spec error: duplicate slug: memory-api/recurring-principles`.

id = "95d4f986-b81c-4951-bae5-4227f2d72a6d"
created_at = "2026-05-20T20:48:15.832511900+00:00"
title = "[audit-api] Include dependency convergence findings in default repo audit"
state = "done"
acceptance_criteria = "1. Repo audit reports dedicated findings when a ticket is waiting on a prerequisite in an earlier workflow state, not just orphan-ticket topology issues. 2. Audit findings import the shared ticket-api dependency-convergence model so counts, state-gap terminology, and affected-dependent reach stay consistent. 3. Default audit output includes machine-readable evidence for prerequisite id or path, dependent id or path, both states, and the relevant convergence metrics. 4. Focused audit-api regression coverage locks both the existing orphan-ticket metric and the new convergence-risk findings without reimplementing graph classification locally."
component = "audit"
priority = "high"
risk_level = "medium"
spec_refs = "4bd3cd3f-5851-4d9e-b499-978cb7b53275"
tags = "audit-api,tickets,dependencies,ranking,convergence"
type = "tracker-improvement"
validation_status = "passed"
validation_summary = "Validation on 2026-05-22: cargo test -p audit-api ticket_graph -- --nocapture passed after the audit documentation polish pass. cargo run -p rule-cli --bin rule -- sync-targets --config rule-targets.yaml regenerated the audit readmes, and cargo run -p rule-cli --bin rule -- sync-targets --config rule-targets.yaml --check passed after updating the audit-cli and audit-mcp source-backed README entries and the shared convergence spec traceability."

Verified against on-disk state 2026-08-19: workflow-tools/spec/ is fully populated (spec crate + spec-api + spec-viewer + tests), not just planned. The stated sequencing risk ("extract only after feedback and test are published") did not block the work -- the actual build resolves unextracted domain deps (feedback-api, test-api, rule-api) via a git bridge straight at the memory-api repo, e.g. rule-api = { git = "https://github.com/mankinskin/memory-api", branch = "main" } (Cargo resolves git deps by crate name anywhere in the target repo, not just at its root). This bridge pattern generalizes: a tool does not need to wait for all dependency-tools to be extracted first. Ticket closed as done; correcting stale board state.
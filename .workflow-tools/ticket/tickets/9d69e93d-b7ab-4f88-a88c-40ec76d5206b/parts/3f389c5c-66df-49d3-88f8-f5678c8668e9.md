## Validation — Typed [[refs]] manifest table

**Scope note:** this ticket's actual ACs (from `get_ticket_description`) differ from the task-prompt's "RESOLVED CONTEXT" summary — the ticket ACs (kind/urn/note, 6 kinds: spec/test_execution/log/rule/file/commit, closed-vocabulary write-time rejection) were treated as authoritative per the escalation instruction, over the prompt's looser `SpecRef`-shaped sketch.

### AC status
1. All six ref kinds round-trip through write, list, and read without loss — **MET**. `TicketFs::write_ref`/`EntityManifest::refs()`/`set_refs()`; test `write_ref_appends_and_round_trips_all_six_kinds`.
2. Unknown ref kind rejected with valid vocabulary in error — **MET**. `ticket_api::model::refs::validate_ref_kind` + `SchemaValidationError::InvalidRefKind`; test `write_ref_rejects_unknown_kind_with_vocabulary_in_error`.
3. Malformed URN for a kind rejected at write time — **MET**. `validate_ref_urn` (per-kind shape checks); test `write_ref_rejects_malformed_urn_for_kind`.
4. Existing `related_specs` migrates to `[[refs]]` with identical spec identity — **MET as transparent read-side bridge**, not a store-wide batch migration (explicitly out of scope per the hard requirements; that is ticket f65f2b32's job). `EntityManifest::refs()` synthesizes `kind="spec"` entries from legacy `related_specs` when no explicit `refs` key exists. Test `legacy_related_specs_extra_bridges_to_refs_with_identical_spec_identity` asserts an empty diff between `related_specs()` ids and `refs()`-derived ids.
5. Reading a ticket whose spec ref points at a deleted spec succeeds and reports dangling — **PARTIALLY MET / DEFERRED**. Reading never fails (guaranteed structurally: `refs()`/`related_specs()` never error). A generic dangling-refs consistency *reporter* across all six kinds was not wired into a CLI/MCP surface in this pass — the existing `ticket-cli validate-links` command already reports dangling `related_specs`/spec refs specifically; extending it to the full `[[refs]]` vocabulary is deferred to keep this change to the model/storage layer, matching the smallest-correct-change instruction. Read-time safety is met; the reporting surface for all 6 kinds is not yet wired.
6. Edge store byte-identical before/after — **MET by non-interference**: `model/edge.rs` was not touched.
7. Test-api validation execution recorded per AC — **MET**: `vt-typed-refs-manifest-table` spec + `exec-typed-refs-manifest-table-20260730` execution recorded, linked to this ticket.

### Schema
```toml
[[refs]]
kind = "spec"
urn = "ce://default/spec/24b3d22b-e235-4c4f-b53c-75fb819ea95b"
note = "contract this ticket implements"

[[refs]]
kind = "file"
urn = "memory-api/crates/ticket-api/src/storage/store.rs"
note = "replace-mode default lives here"
```
(Serialized as an inline `refs = [ {...}, {...} ]` array under the `refs` extra key — same mechanism `[[parts]]` already uses via `manifest_format::write_toml_kv`, not a literal TOML double-bracket table.)

### Files changed
- memory-api/crates/memory-api/src/model/entity.rs — `TicketRefEntry` struct, `EntityManifest::refs()`/`set_refs()`
- memory-api/crates/memory-api/src/error.rs — `SchemaValidationError::InvalidRefKind`/`InvalidRefUrn`
- memory-api/crates/ticket-api/src/model/refs.rs — new: `REF_KINDS`, `validate_ref_kind`, `validate_ref_urn`, `validate_new_ref`
- memory-api/crates/ticket-api/src/model/mod.rs — register `refs` module
- memory-api/crates/ticket-api/src/model/ticket.rs — re-export `TicketRefEntry`
- memory-api/crates/ticket-api/src/storage/ticket_fs.rs — `TicketFs::write_ref`/`remove_ref` (gated write path, same lock + `write_manifest` as `write_part`)
- memory-api/crates/ticket-api/src/storage/ticket_fs/tests.rs — 7 new tests

### Build/test verdict
`cargo build --workspace`: 0 errors. `cargo test -p ticket-api -p memory-api --no-fail-fast`: ticket-api 177/177 passed; memory-api 159/161 passed with the 2 documented pre-existing failures (`discover_workspace_scan_roots_maps_store_roots_to_entity_roots`, `discover_workspace_scan_roots_includes_ancestor_store_roots`), unrelated and unchanged. Real-data sanity: loaded 50 real tickets from `memory-api/.ticket` via `TicketStore`, calling `refs()`/`parts()` on each with no panics (temp test removed after use).

### Deferred
- CLI/MCP/HTTP transport wiring to surface `[[refs]]` writes/lists (create/list-refs commands) — not required by the ticket's ACs, out of scope for "smallest correct change."
- Full dangling-ref consistency reporter across all 6 kinds (AC5 reporting surface) — read-safety is met; extending `validate-links`-style reporting to non-spec kinds deferred as a follow-up.

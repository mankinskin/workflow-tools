## Implementation route

1. Separate link destination parsing, target resolution, repository-boundary detection, guidance-corpus classification, and finding construction into testable functions in the audit trial.
2. Define an explicit portability policy/configuration object that names guidance roots, permitted metadata, nested-repository handling, and non-guidance classes. Do not infer ticket/spec/code status from filename alone when repository metadata can decide it.
3. Preserve current URL, mailto/data, fragment-only, percent-decoding, lexical normalization, exclusion, line-number, and nested-repository behavior unless the new classification deliberately changes a documented outcome.
4. Add stable finding categories and structured evidence fields consumed by both full audit and `audit links`; ensure exit status treats blocking classes as failures.
5. Add a hook-facing report format or stable report path contract that `install-ctl guidance autofix` can consume without scraping human text.
6. Implement autofix as a separate plan/apply pipeline: load findings, verify source content hash, generate only registered safe transformations, show a diff/plan, require explicit apply, write atomically, rerun audit, and rollback on post-check failure.
7. Keep normal installer behavior detection-only. Integration tests must prove that a blocked audit prevents installation and that no implicit autofix occurs.

## Ownership boundaries

- This ticket owns audit classifications, blocking semantics, structured reports, and autofix orchestration.
- Profile/dependency graph semantics belong to ticket `5584da3b`; fixture/Docker execution belongs to `07601b9b`; user documentation/catalog belongs to `5f288155`.
- No semantic content analysis, code-block interpretation, ticket/spec mutation, or automatic deletion of ambiguous references is included.

## Validation Evidence (spec 24b3d22b, ticket 4c7b884e)

Test-api: vt-projected-ticket-reads / exec-projected-ticket-reads-20260730 (passed).

### Build
`cargo build --workspace` — clean (only pre-existing unrelated warnings).

### Tests
`cargo test -p ticket-api -p memory-api -p ticket-cli --no-fail-fast`
- ticket-api: 193/193 passed (incl. 12 new `storage::tests::projection_tests`)
- ticket-cli: 21/21 passed (incl. 2 new CLI reachability tests)
- memory-api: 159/161 passed; 2 failures are the pre-existing, explicitly-excluded `discover_workspace_scan_roots_*` tests — not touched, not fixed, per instructions.

Additionally (not in the required list, but touched by this change):
- ticket-mcp: 8/8 passed (incl. 1 new MCP reachability test).
- ticket-http: 79/81 passed; 2 unrelated pre-existing failures (`serve::routes::tests::ancestor_graph_ref_from_child_workspace_is_followable`, `serve::routes::tests::workspace_graph_includes_isolated_local_and_cross_workspace_nodes`) caused by concurrent, unrelated edits from another agent in this shared workspace (confirmed via `git diff` — not part of this ticket's owned files); not fixed, out of scope.

### Live CLI proof (real tickets in memory-api/.ticket/tickets/, `target/debug/ticket.exe`)

```
ticket get 9d69e93d --view summary --json  -> parts kinds: []          (no objective part on this ticket; absent core kind => empty, not error)
ticket get 9d69e93d --view plan --json     -> parts kinds: [], refs: []
ticket get 9d69e93d --view review --json  -> parts kinds: ['validation']
ticket get 9d69e93d --view full --json    -> parts kinds: ['validation'], refs: []

ticket get 4c7b884e --view summary --json           -> parts: [{kind: objective, implicit: true, content_len: 5893}]  (legacy description-only ticket, implicit objective synthesized)
ticket get 4c7b884e --parts objective,acceptance_criteria --json -> parts kinds: ['objective']  (acceptance_criteria absent -> omitted, not error)

ticket get 4c7b884e --view bogus --json
  -> {"code":"invalid_request","message":"storage error: unknown view profile 'bogus': valid profiles are summary, plan, review, full"}

ticket get 4c7b884e --parts not_a_real_kind --json
  -> {"code":"invalid_request","message":"storage error: unknown part kind 'not_a_real_kind': valid kinds for this ticket are objective, requirements, design, examples, acceptance_criteria, review, validation, notes, amendment, objective"}

ticket get 4c7b884e --view summary --parts objective --json
  -> {"code":"invalid_request","message":"storage error: cannot pass both `view` and `parts`: choose a named view profile or an explicit part-kind list, not both"}
```

### Profile -> kind mapping (single source of truth)

Lives in `memory-api/crates/ticket-api/src/model/parts.rs` (`ViewProfile`, `VIEW_PROFILE_NAMES`):

| profile | kinds() | includes_refs | inlines_amendments |
|---|---|---|---|
| summary | objective | no | no |
| plan | objective, requirements, design, examples, acceptance_criteria | yes | yes |
| review | acceptance_criteria, review, validation | no | no |
| full | None (= every kind present, core + free-form) | yes | yes |

All three transports (CLI `ticket get/describe --view/--parts`, MCP `get_ticket` view/parts fields, HTTP `GET /api/tickets/{id}?view=&parts=`) resolve reads through `TicketStore::project` (`storage/store/projection.rs`), which is the only caller of `ViewProfile`/`ReadProjection`.

## Contradiction resolution (2026-07-30, empirical re-verification)

**Verdict: filtering is NOT broken.** The "later agent" report is the wrong one — traced to a stale-binary/stale-state artifact, not a code defect.

Evidence:
- `~/.cargo/bin/ticket.exe` (globally installed, built 2026-07-30 12:46) hard-errors on `--view` ("unexpected argument '--view' found") — it predates the feature entirely. If invoked via bare `ticket` in PATH it cannot silently ignore the flag; it fails to parse.
- `memory-api/crates/ticket-api/src/storage/store/projection.rs` (the `ReadProjection`/`TicketStore::project` module) was a **net-new file** in commit `6a7a2dcc` ("chore(ticket-api): wip storage and projection updates", 2026-07-30 22:38:51+02:00). Any binary/checkout built or tested before that commit landed had no projection logic to exercise.
- Rebuilt `target/debug/ticket.exe` from current HEAD, tested against the real migrated ticket `e342cc4c-a7a4-42de-81fc-572d0497d12b` (root `.ticket` store, not memory-api's — that ID does not exist in memory-api/.ticket) which has 5 parts (3 objective, 1 review, 1 validation):
  - `--view summary` → 3 parts, all `objective`
  - `--view plan` → 3 parts, all `objective` (same as summary because this ticket has no requirements/design/examples/acceptance_criteria parts — not a bug, profiles legitimately overlap when a ticket lacks the differentiating kinds)
  - `--view review` → 2 parts: `review`, `validation` (correctly excludes objectives)
  - `--view full` → all 5 parts
  - `--parts objective` → 3 objective parts
  - `--parts review,validation` → 2 parts
  - `--view bogus` → clean rejection: "unknown view profile 'bogus': valid profiles are summary, plan, review, full"
  - `--parts nonsense_kind` → clean rejection naming the valid kind vocabulary
- MCP `get_ticket_tool` (memory-api/tools/mcp/ticket-mcp/src/server/query.rs) and HTTP `GET /api/tickets/{id}` (memory-api/tools/http/ticket-http/src/serve/handlers/tickets/read.rs) both call the same `ticket_api::storage::ReadProjection::decode` + `store.project` — no divergence, no per-transport redeclaration of the profile→kind mapping.
- `rtk cargo build --workspace`: clean (0 errors). `rtk cargo test -p ticket-api -p ticket-cli -p memory-api --no-fail-fast`: only the 2 pre-existing, pre-declared path-separator failures (`discover_workspace_scan_roots_*`), unrelated to projection; all ticket-api/ticket-cli/projection tests pass.

No code changes made. Root cause of the contradiction: the "later agent" tested against a pre-`6a7a2dcc` state (stale build and/or stale checkout), not current HEAD.

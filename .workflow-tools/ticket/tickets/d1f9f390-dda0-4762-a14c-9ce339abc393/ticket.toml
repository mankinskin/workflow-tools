id = "d1f9f390-dda0-4762-a14c-9ce339abc393"
created_at = "2026-05-21T15:21:00.837130900+00:00"
title = "[ticket-api][ticket-cli][ticket-mcp] Redesign best-next ranking around dependency convergence"
state = "done"
acceptance_criteria = "1. The ranking redesign spec defines a convergence-first contract and a ticket-api-owned dependency-convergence model that can promote eligible prerequisites when more advanced dependents are blocked behind them. 2. ticket next, ticket-mcp next_tickets, and ticket health or HTTP quality consumers reuse one deterministic graph model from ticket-api instead of maintaining separate traversal or state-gap logic. 3. Audit planning explicitly consumes the same ticket-api model while keeping audit-specific severity and repo reporting in audit-api, and HTTP /api/next remains separate. 4. Focused regression and follow-up planning cover state-gap promotion, transitive scoring, convergence findings, and cross-surface explainability fields."
component = "ticket"
priority = "high"
risk_level = "medium"
spec_refs = "4bd3cd3f-5851-4d9e-b499-978cb7b53275"
tags = "ticket-api,ticket-cli,ticket-mcp,next,ranking,dependencies,convergence"
type = "tracker-improvement"
validation_status = "passed"
validation_summary = "Validation on 2026-05-22: cargo test -p ticket-api workflow:: -- --nocapture passed after the documentation and rustdoc polish pass. Focused ticket-cli, ticket-mcp, ticket-http, and audit-api validations from the implementation pass remain valid, and cargo run -p rule-cli --bin rule -- sync-targets --config rule-targets.yaml --check passed after refreshing the source-backed ticket-cli and ticket-mcp readmes and the convergence spec."

## Build
- `rtk cargo build --workspace`: 0 errors.
- `viewer-ctl prepare ticket-viewer`: succeeds (fixed a pre-existing broken relative CSS path in index.html: 3 `../` levels instead of the required 4, matching doc-viewer/log-viewer's depth).

## Browser verification
Manual + automated verification via Playwright against system Microsoft Edge (Chromium-family), headed, viewport 1280x720.

Screenshots (memory-viewers/ticket-viewer/frontend/dioxus/test-results-release/):
- structured-parts-expanded.png — multiple typed parts (objective/requirements/design/examples/acceptance_criteria/review) as separate sections; objective shows "🔒 Frozen at `planned`"; amendment nested beneath objective labelled "AMENDMENT — SUPERSEDES OBJECTIVE".
- structured-parts-collapsed.png — first part collapsed independently; other sections stay expanded.
- structured-parts-refs.png — typed refs: spec ref renders as a clickable link to spec-viewer (localhost:4002/specs/{id}); a spec ref with a malformed URN renders plain text with an "unresolved" badge; a file ref renders as plain text (no broken link).
- structured-parts-legacy.png — a real legacy ticket (3a1ec9f8, no `[[parts]]`) still renders a single synthesized `objective` section; no blank page, no desc-error.

## Playwright E2E
memory-viewers/ticket-viewer/frontend/dioxus/e2e-release/structured-parts.release.spec.ts — 3/3 passed:
1. "parts render as independently collapsible sections with frozen and amendment markers"
2. "typed refs render as links or dangling markers"
3. "legacy description-only ticket still renders without regression"

Run: `npx playwright test -c playwright.release.msedge.config.ts structured-parts.release.spec.ts` (added a local msedge-channel config override; the default bundled-Chromium download/extraction was unreliable in this sandbox — `npm run test:e2e:release` uses the default config and may need a working Chromium install).

Evidence recorded in test-api: validation spec `vt-ticket-viewer-structured-parts-e2e`, execution `exec-vt-ticket-viewer-structured-parts-e2e-20260730` (passed), linked to this ticket and spec 24b3d22b.

## Deferred / known gaps
- Ref-writing has no HTTP/MCP transport yet (ticket 9d69e93d scope): the refs-rendering fixture ticket (4460cad4-c137-45c4-893e-6e340e16bbe7, memory-api workspace) had its `[[refs]]` table seeded by direct manifest edit, not through a supported write path.
- Running the FULL existing e2e-release suite (not just this new file) currently fails to even load: `ticket-viewer.release.spec.ts` imports `../../../../../viewer-api/viewer-api/frontend/dioxus/e2e/test_apis`, which has the same off-by-one relative-path depth bug I fixed in index.html. Pre-existing, unrelated to this ticket's scope; left unfixed to avoid touching files outside this ticket's owned set.

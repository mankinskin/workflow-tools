Context-enrichment verdict (independent re-verification, no prior session record found via session_sessions_for_ticket at strict/linked/mentioned tiers — this judgment is based solely on independently re-reading the worktree source and attempting an independent bench run, not on trusting the prior review part's self-report).

## Acceptance criterion (from objective part, verbatim)
"cargo bench -p rule-api --bench move_health and cargo bench -p audit-api --bench move_health both run to completion and produce Criterion output for every scenario applicable to that domain's schema."

## Independent verification performed
- Read rule/crates/rule-api/benches/move_health.rs in full: confirmed the scenario-matrix loop (entity_counts [1,25,100,500] x topologies [none,internal,crossing] x densities [0]/[1,5,20] x phases [preflight,apply,rollback], with internal-topology skipped at entity_count<2) computes to exactly 75 scenarios, matching the prior review's claim. Real edge-based link model via EdgeRecord/add_edge, no fabricated collapse — matches ticket's non-goal guidance.
- Read audit/crates/audit-api/benches/move_health.rs in full: confirmed the matrix collapses to entity_count only (1/25/100/500 = 4 scenarios), and independently confirmed the fail-closed rationale in the module doc comment (source_entity_path always None -> MissingSourceEntity blocker -> plan never supported() -> execute_move always errors) is a legitimate, non-fabricated collapse per the objective's own instruction, not a shortcut.
- Attempted to independently reproduce a literal, non-`--test` `cargo bench -p audit-api --bench move_health` run (the smallest of the two crates' matrices) from the worktree. The run did not complete within a 200s bounded window (still compiling/running when checked); no prior evidence trail shows anyone completing a literal (non-`--test`) `cargo bench` run for either crate.

## Verdict: AC NOT met — ticket stays in-review

The recorded validation evidence for both crates is a `-- --test` bounded smoke run (fast dry-run mode that only checks each benchmark closure executes without panicking; it does not run Criterion's sampling/measurement pipeline and does not produce real Criterion output), not the literal `cargo bench` command the AC requires.

- **rule-api: unmet, large gap.** Only 9 of 75 scenarios (12%) were exercised, and only in `--test` mode. The 1-entity tier was itself not fully exercised (9 of that tier's 12 scenarios) before the bounded 90s timeout was hit. The 25/100/500-entity tiers — 63 of 75 scenarios (84% of the matrix) — have zero execution evidence in any mode. This is a genuine, code-verified gap, not a nitpick: the majority of the claimed matrix has never actually been run.
- **audit-api: smoke-only, no confirmed full run.** All 4 applicable scenarios passed in `--test` mode, which is good evidence the code paths are correct, but no literal `cargo bench` (non-`--test`) run has been completed by any prior session or by this verification attempt (my own 200s-bounded attempt did not finish).

Since the AC requires **both** crates' bench targets to run to completion with real Criterion output for every applicable scenario, and rule-api plainly fails this (84% of its scenario matrix untested in any form), the criterion is unmet regardless of audit-api's stronger but still incomplete evidence.

## Compounding factor (secondary, not the primary blocker)
The implementation exists only on an uncommitted worker branch (agent/bb192c09/rule-audit-move-benchmarks, worktree workflow-tools/.worktrees/bb192c09-rule-audit-move-benchmarks) in both the rule and audit submodules. Even once the coverage gap above is closed, the work must be committed and merged before this ticket can be considered delivered — closing to `done` while nothing has landed would misrepresent ticket state.

## What would close this
A full (non-`--test`) `cargo bench` run of both crates' `move_health` bench, with the run allowed enough wall-clock budget to reach the 500-entity tier for rule-api (its 75-scenario full matrix, not a subset), plus committing the work in both submodules. Given the fixture-setup cost noted inline in rule-api's own bench module docs ("Fixture setup ... dominates cost at the upper end of the matrix"), expect the rule-api full run to need a materially larger timeout budget than the 90s used so far.
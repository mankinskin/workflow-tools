Implemented via delegated Implement Agent worker on branch agent/bb192c09/rule-audit-move-benchmarks (fresh worktree workflow-tools/.worktrees/bb192c09-rule-audit-move-benchmarks, created from workflow-tools main to avoid overlap with ticket 6d4ac04f's worktree).

Files changed:
- rule/crates/rule-api/Cargo.toml (criterion dev-dep + [[bench]] move_health)
- rule/crates/rule-api/benches/move_health.rs (new)
- audit/crates/audit-api/Cargo.toml (criterion dev-dep + [[bench]] move_health)
- audit/crates/audit-api/benches/move_health.rs (new)

Behavior:
- rule-api: full scenario matrix (entity_count {1,25,100,500} x topology {none,internal,crossing} x density {0/1/5/20} x phase {preflight,apply,rollback}) against real RuleStore::create / entity_store().add_edge / plan_move_preflight / execute_move_with_journal / rollback_move_with_journal. No axis collapse -- rule has a real edge-based link model.
- audit-api: matrix intentionally collapsed to entity_count only (1/25/100/500), single no_links topology, single preflight phase, documented inline. Reason: AuditMoveDomain is fail-closed -- source_entity_path is always None (MissingSourceEntity blocker, plan never supported()), related_entities always empty, so link topology/density collapse to one case and apply/rollback are structurally unreachable (execute_move errors on any unsupported plan) rather than fabricated.

Validation evidence (compile, clean):
- cargo check --bench move_health in rule/crates/rule-api -> Finished, no warnings in new code
- cargo check --bench move_health in audit/crates/audit-api -> Finished, no warnings in new code
- cargo check -p rule-api / -p audit-api -> Finished clean (pre-existing warnings elsewhere unrelated)

Validation evidence (bounded smoke runs):
- timeout 90 cargo bench --bench move_health -- --test (rule-api): bench-profile compile alone took 55s of the 90s budget; 9/75 scenarios ran, all Success (all 1entities_* combinations across no_links/crossing_links topologies, both phases through rollback). Killed cleanly at 90s deadline -- only the 1-entity tier was exercised end to end; 25/100/500-entity tiers not reached in this bounded run (same code path, untested tiers are a coverage gap in this session's evidence, not a known defect).
- timeout 150 cargo bench --bench move_health -- --test (audit-api): completed in full within budget (compile 1m09s + run ~1s), all 4 scenarios (1/25/100/500entities_no_links_preflight) Success, exit 0.

Gap: rule-api's full matrix (75 scenarios) was not exercised beyond the 1-entity tier within the bounded timeout used this session; a longer-budget rerun would confirm the remaining tiers but was not attempted per the mandatory bounded-timeout policy. No commit was made in either worktree; both are pending a follow-up commit/merge step.
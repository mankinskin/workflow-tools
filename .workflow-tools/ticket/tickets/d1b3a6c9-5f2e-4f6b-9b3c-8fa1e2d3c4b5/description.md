- session-api: removed diag_* bubble-node emission from render_workflow_mermaid (store.rs), dropped its now-unused diagnostics param, updated both call sites (store.rs, store/config/workflow.rs). Added `SessionWorkflowValidationIssue` + `validate_workflow_graph` (dangling-edge, duplicate-node-id detection) in model/workflow.rs, re-exported from model.rs and lib.rs. Added SessionError variants `WorkflowGraphInvalid` and `WorkflowDiagnosticsUnresolved`. create_handoff_record (store/config/handoff_finish.rs) now runs validate_workflow_graph right after workflow_snapshot and blocks (returns Err before any file write) on structural issues or unresolved resolver diagnostics. session-mcp server.rs session_err maps both new variants to invalid_params. Added tests: create_handoff_result_rejects_dangling_edge_before_writing_files, create_handoff_result_rejects_unresolved_diagnostics_before_writing_files, validate_workflow_graph_reports_no_issues_for_valid_graph, validate_workflow_graph_detects_dangling_edge, validate_workflow_graph_detects_duplicate_node_id.
- audit-api: new trial memory-api/crates/audit-api/src/trials/session_workflow_graph.rs reusing session-api's validate_workflow_graph against `.session/sessions/*/context.json` and `.session/sessions/*/handoffs/*/handoff.json`; registered in trials/mod.rs; new AuditMetrics.session_workflow_graph field; wired into audit.rs; session-api added as a Cargo dependency. Test: evaluate_reports_dangling_edge_in_context_json.
- Validation (corrected 2026-07-29 after review): `cargo test -p session-api` all suites pass; `cargo build -p audit-api` clean; `cargo test -p audit-api` **13 passed, 1 failed** — `trials::rule_overlap::tests::reports_high_overlap_between_near_duplicate_rules` fails deterministically with `DuplicateSlug("shared/prompts/handoff-a")` on code untouched by this ticket. Tracked separately as memory-api workspace ticket e5e3b293 (Fix audit-api rule_overlap DuplicateSlug test failure and correct d1b3a6c9 validation evidence); user approved keeping this as a non-blocking follow-up since the new session_workflow_graph trial test itself passes.
- Note: existing None-resolver test callers were unaffected since diagnostics stay empty when no resolver is supplied.
- Note: the new validate_workflow_graph gate in create_handoff_record blocks on ANY unresolved ticket-state diagnostic, including Optional-requirement or stale/unrelated workflow-graph nodes (e.g. a stale track left in a session's graph after its track changes). User confirmed this is intended (loud-by-design diagnostics; pruning stale nodes is a board-hygiene action via session_workflow_remove_node, not a code defect) — no follow-up opened for this.
- Cross-workspace note: e5e3b293 lives in the memory-api ticket store; add_edge could not link d1b3a6c9 (default-workspace-visible) to e5e3b293 (memory-api-workspace-visible) due to the same unsupported cross-workspace ticket routing limitation observed elsewhere — tracked by reference in this description instead of a formal graph edge.
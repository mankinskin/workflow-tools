Define queue explainability and reversible cleanup action semantics for continuous store-health workflows.

Acceptance criteria:
- queue items expose metric contributions and recommended-action rationale
- cleanup actions include reversibility/provenance metadata
- ordering remains deterministic for a fixed scoring model version
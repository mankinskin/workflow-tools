Passed: `cargo test -p worktree-ctl --test worktree_contracts bootstrap -- --nocapture` (4 passed).

Passed: `cargo test -p worktree-ctl --test worktree_contracts -- --skip guidance_documents_nested_legacy_and_worktree_local_active_session_marker` (17 passed, 1 filtered).

Known baseline blocker: the full `cargo test -p worktree-ctl` suite fails only because `guidance_documents_nested_legacy_and_worktree_local_active_session_marker` expects `.session/local/active_workspace_session.json` in the untouched `session-identity-and-handoff.instructions.md`; the current guidance and its `HEAD` version do not contain that retired marker.
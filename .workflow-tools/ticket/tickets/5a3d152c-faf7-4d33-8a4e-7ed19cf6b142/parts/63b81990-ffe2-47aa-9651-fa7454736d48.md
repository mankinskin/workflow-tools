Iteration review (this session) — returned to in-implementation.

FAIL: AC2 (invalid core kind must be rejected) is unimplemented. `classify_part_kind` (memory-api/crates/ticket-api/src/model/parts.rs) never fails — any string is accepted as a valid attachment kind, and `EntityTypeSchema::validate_manifest` (memory-api/crates/memory-api/src/model/schema.rs#L63) only checks required fields, not core-kind membership.

User decision: genuine gap, not a descope. Add real core-kind validation so an invalid/unknown core kind is rejected (not silently treated as a free-form attachment), then update/add a test proving rejection.

Also FAIL: AC7 test-api evidence. Only a bespoke `exec-vt-ticket-parts-storage-model-20260730` execution exists; the epic's own required validation spec `vt-structured-ticket-entities-rust` has zero recorded executions. User decision: record real executions against `vt-structured-ticket-entities-rust` before closing (do not just relabel the bespoke id).
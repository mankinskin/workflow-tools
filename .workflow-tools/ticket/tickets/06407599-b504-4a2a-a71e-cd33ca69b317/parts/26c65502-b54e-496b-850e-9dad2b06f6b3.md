`session-cli handoff` hardcodes the handoff `package` argument to `None` (memory-api/tools/cli/session-cli/src/lib.rs, `SessionCommand::Handoff(args)` arm, line 695), so handoff packages cannot be supplied over the CLI transport even though the underlying session-api (`create_handoff_result`) supports them. `HandoffArgs` (memory-api/tools/cli/session-cli/src/lib.rs, line 549) has no field for supplying a package payload at all. Carried over from session 5b961940 and still unticketed as of 2026-07-30.
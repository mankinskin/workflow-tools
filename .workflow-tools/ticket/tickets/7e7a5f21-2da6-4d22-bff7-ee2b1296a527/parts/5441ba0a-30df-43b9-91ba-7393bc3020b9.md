- A migration path exists that converts a `planned` ticket's legacy description into typed parts without unfreezing/bypassing `enforce_part_write_gate` (memory-api/crates/ticket-api/src/storage/store/parts.rs:37) and without round-tripping the ticket's state out of and back into `planned` (e.g. an explicit frozen-aware apply path, or a documented manual/audited exception in the store layer).
- `migration_undo` and `TicketFs::remove_parts` (memory-api/crates/ticket-api/src/storage/store/migration.rs:353, :382) were implemented as part of f65f2b32 but have never been exercised against a real ticket — only unit-tested in isolation; this ticket must add an end-to-end exercise.
The store-wide description→parts migration (ticket f65f2b32) deliberately skips tickets currently in state `planned`, because planning parts on a `planned` ticket are frozen by `enforce_part_write_gate`, and stepping a ticket out of `planned` and back to bypass the freeze hit an unrelated dependency-state gate (real ticket `e342cc4c` was manually recovered during epic bbb4bce9 validation). Need a safe, auditable migration path for `planned` tickets that doesn't unfreeze or round-trip state.
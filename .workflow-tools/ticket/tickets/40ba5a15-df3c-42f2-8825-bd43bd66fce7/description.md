# Objective

Group the ticket CLI, board, MCP, and next-work backlog items under one parent so the discovery and workflow contract is represented in the dependency graph.

# Scope

- Ticket CLI and multi-root behavior.
- Board and next-work workflow integration.
- Frontend surfaces that depend on the next-work contract.

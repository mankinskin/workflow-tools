- After a session dispatches at least one sub-agent, `.session/local/hook-captures/SubagentStart.json` and `.session/local/hook-captures/SubagentStop.json` both exist and contain `agent_id` and `agent_type`.
- `session.exe subagent-rollups --session-id <id>` returns one row per sub-agent dispatch for that session.
- The root cause of the missing session record for `b9020ba2-df5d-426a-b1b9-228ef159cad1` is stated, including the expected path and the path that actually exists.
- A documented, repeatable procedure answers "which dispatches in session X failed and why" using repository tooling alone, with no manual JSONL reading.
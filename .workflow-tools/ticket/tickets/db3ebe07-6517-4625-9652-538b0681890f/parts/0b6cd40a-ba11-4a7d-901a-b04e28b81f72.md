
## Resolution (2026-09-01)

User decision: "." is now the explicit "current directory" selector, resolved relative to the MCP server process's cwd. Implemented:

- memory-kernel/src/workspace.rs: validate_explicit_workspace_selector no longer rejects "."; still rejects omitted/empty/"default"/"..". Error message updated to state '.' targets the MCP server process's cwd.
- memory-kernel/src/workspace_tests.rs: split into explicit_workspace_selector_rejects_ambient_aliases (no longer includes ".") and a new explicit_workspace_selector_accepts_current_directory test.
- Updated the matching embedded tests and the "Do not use omitted, empty, 'default', '.', or '..'" doc comment (33 occurrences) in: session/src/mcp/server.rs (26 doc comments + 1 test), rule/src/mcp/server.rs + types.rs (2 doc comments + 1 test), feedback-mcp/src/server.rs (2 doc comments + 1 test), test-mcp/src/server.rs (2 doc comments, no local test), spec/src/mcp/server/types.rs (1 doc comment, no local test).
- Found and fixed a pre-existing, unrelated bug hit while testing: rule's Cargo.toml `mcp` feature was missing `dep:uuid`, causing a compile failure under `cargo test -p rule --features mcp` (mcp/server.rs uses uuid::Uuid at two call sites). Fixed by adding dep:uuid to the mcp feature list.
- Validated: cargo test passes for memory-kernel (2/2), session --features mcp (2/2), rule --features mcp (2/2, after the uuid fix), feedback-mcp (3/3). spec compiles cleanly (no local test existed for this).
- Found and filed as a separate follow-up (NOT fixed here, out of scope for this change): test-mcp's list_executions_aggregates_nested_descendant_store fails pre-existing and unrelated - see ticket 6cf90b00 (resolve_store_root_at_workspace silently upgrades an explicit legacy store path to canonical when neither exists yet).

## Remaining scope (unchanged from original ticket, not done in this pass)

Part A (workspace_slug default elimination in feedback-http/mcp, test-cli/mcp, mcp-toolmon's anchored_resolver) is still open. This pass only resolved part B (the "." design question) and its implementation.

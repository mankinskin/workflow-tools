Discovered while reviewing d1b3a6c9. `cargo test -p audit-api` intermittently fails on `trials::rule_overlap::...reports_high_overlap_between_near_duplicate_rules` with a `DuplicateSlug` panic on the *first* insert into a fresh tempdir-backed `RuleStore`. Reproduces alone and single-threaded, unrelated to session-api/audit-api workflow-graph validation work. Root cause suspected: `RuleStore::init` picks up rule state from outside the tempdir it was given (test isolation leak). Blocks d1b3a6c9 from reaching done per user decision (audit-api evidence must show a clean 14/0 run).
Mechanical removal of the `workspace_session_id` identifier now that `session_id` is the single identity handle (see spec 709f067a-21b6-41b6-8879-3cacef4bacaf). The string occurs in 28 files, ALL under memory-api/crates/session-api/ — it appears in NONE of session-capture-hook, session-worktree-provision, or session-workspace-resolver, so the blast radius is confined to session-api. Highest-occurrence files: tests/handoff_folder_storage.rs (38), src/store/config/runtime_workflow.rs (31), src/store/config/handoff_finish.rs (28), src/store/config/workflow.rs (24), src/store/config/worktree_runtime.rs (22), tests/handoff_roundtrip.rs (22), src/store/config/persistence.rs (11), src/error.rs (10). Must preserve backward-compatible READ of legacy slug-keyed runtime-context directories. Depends on the correctness fix in 76c64b38.
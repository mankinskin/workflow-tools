Implemented and validated on this Windows/Git-Bash host (no Developer Mode/elevation available):

- tools/install/sync-copilot-surfaces.sh: each `.github/{agents,prompts,instructions}` dir now tries `ln -s` to the corresponding `.agents/` source first. Detection uses `-L` (not just `-d`), because on this host `ln -s` for a directory can return exit 0 while silently falling back to a full recursive copy instead of a real symlink when it lacks privilege — `-d` alone would have been fooled by that.
- Fallback (this host, confirmed no Developer Mode via PowerShell `New-Item -ItemType SymbolicLink` -> "Administrator privilege required"): recursive copy + `chmod -R a-w`. Verified: writing to `.github/agents/Default.agent.md` afterwards fails with "Permission denied".
- `git check-ignore -v` on `.github/agents`, `.github/prompts`, `.github/instructions` still reports all three ignored via the existing `/.github/agents/` etc. .gitignore patterns, regardless of whether the entry is a symlink or a real directory.
- Could not exercise the symlink success path on this host (no admin/dev mode). Logic reviewed to be correct standard bash (`ln -s SRC DEST`, `-L`/`-d` checks); relies on Linux/macOS/Windows-with-dev-mode to take that branch.

Commit: c2f72d12 "sync-copilot-surfaces: try symlink, fall back to read-only copy" on branch agent/b9020ba2-df5d-426a-b1b9-228ef159cad1/session.

No spec change needed — this is a build-tooling script behavior fix, not a product requirement/goal change.
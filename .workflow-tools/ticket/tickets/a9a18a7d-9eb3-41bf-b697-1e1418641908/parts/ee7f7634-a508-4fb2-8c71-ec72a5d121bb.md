Review verdict: pass. Recommended target state: done. No state transition was applied by Review Agent.

Verified criteria:
1. STORE_MARKERS covers ticket, spec, rule, test, audit, session, and feedback. The matrix test exercises canonical-only, legacy-only, both-present, and neither-present for each. The amendment assigns log to 55b90291 and excludes doc because it has no modeled store.
2. Legacy-only reads return the legacy root with StoreRootDiagnostic::LegacyStore and the canonical .workflow-tools/<domain> destination.
3. Initialization helpers use canonical_store_root and never return legacy paths.
4. Canonical discovery recognizes session and feedback, reports canonical roots once, and prunes store internals, .git, target, and node_modules.
5. Existing flat-layout tests remain and the compatibility read resolver is exercised.

Reviewer validation in workflow-tools/memory-kernel:
- cargo test -p memory-kernel: passed, 169 tests.
- cargo build -p memory-kernel: passed.
- cargo clippy -p memory-kernel --all-targets -- -D warnings: 29 findings across the package. Reviewer approved these as non-blocking.
- git diff --check: passed.

Scope check: the memory-kernel diff is limited to src/workspace.rs, src/discovery.rs, src/model/index_entry.rs, src/model/urn.rs, and src/workspace_tests.rs.
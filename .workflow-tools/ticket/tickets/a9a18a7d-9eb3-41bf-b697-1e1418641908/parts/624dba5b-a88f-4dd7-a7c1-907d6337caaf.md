## Requirements

- The shared resolver owns the canonical domain-name mapping and legacy candidates.
- A read result exposes whether it is canonical or deprecated legacy resolution.
- Resolver callers cannot accidentally create a flat legacy root.
- Discovery recognizes both layouts during the compatibility period, with canonical stores preferred.
## Requirements Amendment (review, 2026-08-26)

Adds to the frozen requirements above; does not replace them.

- `STORE_MARKERS` in [workflow-tools/memory-kernel/src/discovery.rs](../../../../workflow-tools/memory-kernel/src/discovery.rs) currently recognizes only `.ticket`, `.spec`, `.rule`, `.test`, and `.audit`. This ticket must extend discovery to recognize session and feedback stores too (log is covered separately per ticket 55b90291's log-domain requirement; doc is out of scope per the spec/tracker amendment dropping doc pending a real store), so canonical nested-store discovery (AC4) is not limited to the five domains recognized today.
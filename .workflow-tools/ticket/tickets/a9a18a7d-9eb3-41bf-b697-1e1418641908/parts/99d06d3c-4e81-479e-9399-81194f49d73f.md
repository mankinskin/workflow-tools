## Design

Introduce one shared `StoreLayout`/resolver abstraction with canonical and legacy candidates. Extend recursive discovery to recognize stores below `.workflow-tools/` without recursing into store internals. Return an explicit ambiguity result when both candidates exist, and attach a deprecation diagnostic to legacy-read resolution. Keep per-domain entity schemas and cross-workspace move semantics unchanged.
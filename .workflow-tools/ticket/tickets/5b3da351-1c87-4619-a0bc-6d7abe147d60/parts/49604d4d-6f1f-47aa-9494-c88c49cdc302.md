Iteration review (this session) — returned to in-implementation.

Overall PASS: strongest ticket in the batch (8 independent per-AC test-api executions, live re-verification confirms zero remaining `state = "new"/"ready"` in any ticket.toml). Returned to in-implementation solely per user decision to fix the ticket's own disclosed gap rather than leave it untracked as a separate follow-up: 2 stale lines in `.rule/README.md` still reference the old `new`/`ready` state names. Update those lines to match the `open`/`planned` rename, then return to in-review.